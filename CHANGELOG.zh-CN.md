# 更新日志

## 3.0.0 - 2026-08-25

### 破坏性变更

- 将所有 Maven 源码模块迁移到 `modules/`，相关模块构建路径需要同步更新。
- 将播放实现类拆分到 `camera`、`event`、`timeline` 和 `track` 子包。

### 新功能

- 新增可扩展、基于帧的播放会话与轨道注册 API。
- 为 Java 客户端新增显示实体镜头后端，支持配置插值和锚点策略。
- 新增位置与旋转插值模式，并引入弧长参数化和 Catmull-Rom 样条。

### 重构

- 分离源码模块、项目文档、集成测试与本地测试服产物。

### 测试

- 新增播放生命周期、镜头传输、事件调度、时间线、插值和玩家状态恢复的 Java 单元测试。
- 新增 Mineflayer 播放内核与显示镜头集成测试场景。
